<h1><%= @user %>'s Tweets</h1>

<%= link_to 'Home', tweets_path %>

<div>
  <ul>
    <% @tweets.each do |tweet| %>
      <% if tweet.user == @user %>
        <li>
          <%= tweet.content %>
        </li>
      <% end %>
    <% end %>
  </ul>
</div>
